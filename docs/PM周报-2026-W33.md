# PM 周报 2026-W33（2026-08-15）

> 范围：健康体检 / 数据生命周期 / 数据飞轮修复 / 首启引导与按钮反馈 / UI 走查与改版

## 一、本轮结论

工作台从「能跑」推进到「可被小白直接使用」：新增数据管理入口与首启引导，修复了 3 个真实 bug（其中 1 个会导致设置弹窗在部分页面打不开），数据飞轮「已应用」从语义混乱变为真实状态。全量测试 157 项通过，公开快照 67 项通过，安全门禁 PASSED，浏览器冒烟 0 JS 报错。

## 二、缺陷清单（发现 5 项，已修复 5 项）

| 级别 | 问题 | 修复 |
|---|---|---|
| P0 | 设置/引导/SOP 弹窗被放在隐藏的「流水线」视图内，非流水线页打开时不可见 | 弹窗移到 body 顶层，任意视图可显示 |
| P1 | 三平台周经验 ID 重复，导致「已应用」点错对象、KPI 计数失真 | ID 按平台唯一化；存量数据已迁移 |
| P1 | 爆款「已应用」无任何 UI 入口，KPI 永远为 0 | 已拆解记录新增「标记应用」按钮 |
| P2 | Pydantic 弃用告警（`.dict()`） | 改为 `model_dump()` |
| P2 | 授权升级地址残留已失效的 mianbaoduo.com | 改为 mbd.pub，支持环境变量覆盖 |

## 三、功能与体验优化

1. **数据管理（呈现 / 保留 / 清理）**
   - 新增 `GET /api/retention/status` 与 `POST /api/retention/apply`；
   - 设置页新增「数据管理」面板：显示占用、可释放空间、各类可清理项、上次清理时间，一键清理；
   - 保留规则（已有引擎）：日志 7 天、榜单快照 90 天、旧任务 30 天归档、未出爆款任务的图片 90 天清理；文案与拆解报告永久保留；每日流水线 dry-run、周日自动清理。
2. **首启引导**：新用户打开工作台弹出「3 分钟跑通」4 步引导（配 AI → 看选题 → 采纳生产 → 查看成品），可跳过、可随时从侧边栏 🚀 重开。
3. **按钮反馈统一**：采集榜单 / 批量拆解 / 周经验包 / 反哺生成 / 保存配置 / 测试连接 / 激活授权 / 数据清理等重操作全部接入加载态（禁用 + 转圈），消灭「点了没反应」。
4. **数据飞轮语义**：`applied=true` 现在表示「这条经验已真实写入 Agent SOP」；反哺升级完成后自动置位，KPI 与事实一致。当前 6/6 条经验已应用。
5. **UI 走查**：概览副标题更新为「生产 + 分析 + 学习闭环」；首页/选题页/引导弹窗视觉 QA 通过，无错位、无遮挡、无空白。

## 四、验证数据

- 私有仓库测试：**157 passed**（+2 新增用例：经验 ID 唯一性、retention API 往返）
- 公开快照测试：**67 passed**；`check_public_repo.py`：**PASSED（0 项）**
- API 冒烟：`/`、`/api/license/status`、`/api/retention/status`、`/api/retention/apply`、`/api/viral`、`/api/topics`、`/api/flywheel` 全部 200
- 浏览器冒烟：无头 Chromium 加载 0 console/page error；引导弹窗 DOM 与视觉双重确认可见
- 实际执行一次数据清理：扫描 76.25MB，当前无过期数据可释放（符合预期）

## 五、遗留事项与风险

1. **重启工作台生效**：8787 上运行的旧实例不含本轮改动，需要重启后才能看到新 UI（本轮测试实例 8788 已关闭）。
2. **待用户材料**：面包多真实商品链接、微信二维码、公众号是否认证、真实 LLM Key 实测。
3. **私有 git 基线不同步**：工作区有约 87 个未跟踪文件（此前多轮功能从未提交），建议后续做一次「基线提交」决策（哪些进库、哪些排除），避免产品代码只存在于工作区。
4. 公开仓库本轮改动尚未推送 GitHub（等待你的发布指令）。

## 六、下一轮建议

- 爆款跟踪 / 数据飞轮 / 成品库三页做视觉走查与交互补全；
- 真实环境验收：一次完整「选题 → 生产 → 发布 → 48h 回填 → 飞轮反哺」；
- 面包多商品上线并替换 README 占位；
- 决定并执行私有仓库基线提交。

---

## 七、第二轮补充（2026-08-15 下午）

### 已交付

1. **工作台重启**：8787 已切换到本轮新代码（含数据管理、首启引导、动效）。
2. **基线提交**：`ba3e254`，279 个文件入库（41,516 行），排除运行日志与密钥。
3. **动效补齐**：弹窗/卡片/toast 入场动画；按钮加载态（转圈 + 禁用）覆盖全部重操作。
4. **三页视觉走查**：爆款跟踪 / 数据飞轮 / 成品库截图视觉 QA 通过，0 JS 报错、无遮挡错位。
5. **修复一个隐藏 bug**：任务名含 `.` 或空格（如 `AI产业1.2万亿`、`xAI发布Grok Bo`）在成品库详情请求时返回 400，原因是 job_id 白名单过严；已放开 `.` 与空格并加单测，三页冒烟从 1 个 400 降为 0。
6. **真实端到端验收通过**：
   - 选题：采纳「免费开源AI模型推荐」（评分 17.7）；
   - 生产：Codex 全流水线 12 分钟跑完（选题→素材→成稿→视觉→质检→归档），质检 100/100、契约 PASSED、去 AI 味 PASSED；
   - 产出：小红书 4 卡 + 文案、公众号排版 HTML、短视频 120s 分镜脚本；
   - 发布：已标记人工发布；
   - 回填：3200 阅读 / 186 赞 / 95 藏 / 32 评（验收样例值，互动率 9.8%）；
   - 反哺：重新生成反哺指令包，4 份 Agent SOP 版本递增（含新增内容合规审核 Agent）。

### 待用户材料（GitHub 发布前）

- 面包多真实商品链接；
- 微信二维码图片；
- 公众号认证状态；
- 真实 LLM Key 设置页实测一次。
