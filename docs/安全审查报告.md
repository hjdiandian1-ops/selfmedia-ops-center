# 发布前安全审查报告（五层门禁）

> 项目：自媒体运营工厂 ｜ 审查日期：2026-08-14 ｜ 审查对象：当前工作区 + 公开仓库快照 `release/selfmedia-ops-center`
> 结论：**公开仓库快照通过全部门禁（0 高险发现）**；私有工作区存在若干「已排除或已接受」项，见第 7 节。

## 1. 工具与版本

| 工具 | 版本 | 用途 |
|---|---|---|
| gitleaks | 8.24.3 | 秘密扫描 |
| truffleHog | 2.2.1 | 已验证秘密扫描（only-verified） |
| detect-secrets | 1.5.0 | 秘密扫描（高熵/关键词） |
| bandit | 1.9.4 | Python SAST |
| semgrep | 1.173.0 | 规则引擎 SAST |
| OSV API（自研 osv_audit.py） | — | 依赖漏洞（pip-audit 在受管 Python 下 ensurepip 异常，改用 OSV 直查） |
| pip-licenses（piplicenses） | 5.5.5 | 许可证审计 |
| 自研 check_public_repo.py | — | 发布前快照校验 |

## 2. 第一层：秘密与脱敏

### 扫描结果
- **gitleaks（工作区全量）**：13 处 generic-api-key，全部位于 `jobs/2026-08-13_Grok 4.6性价比炸/production.log` 与 `skills/dbskill/知识库/原子库/atoms*.jsonl`——两者均不在公开仓库白名单内（jobs=真实数据、dbskill=第三方 skill）。
- **gitleaks（公开快照）**：**0 泄漏**。
- **truffleHog --only-verified**：**0 已验证凭据**。
- **detect-secrets**：5 处（Feishu 集成文档 1 + 历史 validate_report 1 + scripts/_archive 3），全部位于排除目录。
- **自定义正则兜底**：公开快照 0 命中（API key 前缀、Bearer、私钥块、手机号、邮箱、内网 IP）。

### 处置
- `scripts/fetch_share_url.py` 硬编码的 WorkBuddy 分享 token 长链接 → 已改为环境变量 `WORKBUDDY_SHARE_URL` 注入，代码内仅保留占位符。
- 内网 NAS 地址 `192.168.x.x` 仅存在于 `nas_config.py` 默认值与 materials 热点文件（私有数据，不进公开仓库）；`nas_config.py`、`fetch_hot_topics.py`（含 NAS/代理逻辑）不在公开白名单。
- 公开仓库 `git ls-files` 25 个文件，无 jobs/outputs/materials/data/nas-n8n 真实文件。

## 3. 第二层：依赖漏洞

- `requirements.in` → pip-compile 生成 `requirements.lock`（32 个固定版本依赖）。
- OSV 直查：**32/32 依赖 0 已知漏洞**（结果 `/tmp/osv.json`）。
- pip-audit 本机因受管 Python ensurepip SIGABRT 无法运行；CI 使用标准 Python 运行 `pip-audit -r requirements.lock`（github workflow security.yml）。

## 4. 第三层：代码漏洞（SAST）

### bandit
- 修复：3 处 B324（MD5 用于 ID 生成，非安全用途）→ 全部改为 `usedforsecurity=False`。
- 剩余 HIGH（61 处）经 triage：B507/B601（paramiko 自动信任主机 + SSH 命令）集中在 NAS/n8n 运维脚本与 `scripts/_archive/`，**不在公开仓库白名单**，属内部工具接受风险；建议后续为 SSH 增加 host key 固定（记录为待办）。
- 核心公开文件（security_utils / ai_flavor / compliance / license / server）bandit HIGH=0；仅 1 处 B310（OSV 固定官方地址）加 `# nosec` 说明。

### semgrep（auto 规则，30 条 WARNING）
- `dynamic-urllib-use-detected` / `insecure-request-object`：集中在 NAS/飞书/图像生成等私有脚本，URL 来自常量或环境变量，无用户输入；公开模块 `collect_platform_virals.py` 已加 SSRF 白名单防护。
- `playwright-goto-injection`（render_card_to_image.js）：本地固定路径，私有脚本，已记录。
- `insecure-urlopen`（osv_audit.py）：固定官方 API 地址，已 `# nosec`。

### 五类攻击面复核（公开/工作台代码）
| 攻击面 | 结论 |
|---|---|
| 命令注入 | `job_id`/`theme` 统一白名单正则（security_utils），已接入 job_state、server 全部 job_id 端点与 adopt 建任务 |
| SSRF | `safe_http_url()` 拒绝内网/环回/链路本地/云元数据；接入 fetch_http、collect_platform_virals._http_get、viral analyze 链接校验 |
| 路径遍历 | api_qa output_dir 归一化 + basename 白名单；job_id 端点全部白名单；静态目录仍由 Starlette StaticFiles 边界保护 |
| 恶意文件 | xlsx 导入增加 zip 炸弹防护（大小/条目/单成员/压缩比上限）；ElementTree 不解析外部实体（文档记录） |
| XSS/无鉴权 | webapp 仅绑 127.0.0.1；README/SECURITY.md 明示「本地单机，禁止公网直连」 |

## 5. 第四层：供应链与许可证

- pip-licenses：146 个已安装包；运行时锁文件许可证以 MIT/Apache-2.0/BSD 为主；copyleft 仅有 **paramiko（LGPL-2.1，作为依赖可接受）**；semgrep（GNU）为开发工具不进锁文件。
- 第三方 skill（dbskill/gzh-design/guizang/xiaowan-wechat-layout）各自带 LICENSE，**一律不进公开仓库与付费包**，README 仅提供外部链接与原 LICENSE 说明。
- 付费包仅含自有原创内容（调研结论见 `research/去AI味开源调研.md` 的许可说明）。

## 6. 第五层：仓库与 CI 安全

- 公开快照已创建：`SECURITY.md`、`CONTRIBUTING.md`、`SECURITY_CHECKLIST.md`、`.github/workflows/security.yml`（gitleaks + pip-audit + bandit + semgrep 四道闸门）、MIT LICENSE、加固 .gitignore。
- 快照为全新 git 历史（1 个初始提交），不携带本仓库历史。
- GitHub 仓库侧设置（secret scanning/push protection、Dependabot、CodeQL、分支保护、签名提交、immutable release）需在创建仓库后由用户/执行者按 `SECURITY_CHECKLIST.md` 开启——当前本机 gh token 已失效，**无法自动创建/推送**。
- 提交身份：快照提交使用 `xiaowuliao@users.noreply.github.com`（noreply），不泄露个人邮箱。

## 7. 残留风险与人工确认项

| 项 | 等级 | 说明 |
|---|---|---|
| NAS/paramiko 脚本未做 host key 固定 | 中 | 内部工具，不进公开仓库；待办：SSH 增加 known_hosts 固定 |
| 离线 token 无法防专业破解 | 中 | 设计目标为防普通用户共享；v2 在线激活/吊销已预留接口 |
| 闲鱼渠道风控 | 中 | 已按「授权激活服务」话术上架，主渠道放面包多 |
| 依赖自动更新 | 低 | 需定期重跑 pip-compile + OSV 审计（建议月度） |

**发布确认（需用户逐项签字）**
- [ ] 公开仓库白名单与扫描结果无异议
- [ ] 授权/定价/渠道按商业计划书 v1.0 执行
- [ ] gh auth login 后由执行者创建 `selfmedia-ops-center` 并推送（推送前再跑一次 check_public_repo.py）

## 8. 交叉审查整改记录（2026-08-14 第二轮）

第三方交叉审查（ClaudeCode 全仓克隆逐文件复核）发现并已修复：

1. 泄漏本机绝对路径：`demo/ai_flavor_report.json` 的 `output_dir` 含 `/Users/xiaowuliao/...`。根因是 `ai_flavor_check.py` 把传入路径原样写报告。已修复：`run()` 相对化 `output_dir`、demo 生成器改传相对路径、`check_public_repo.py` 与 `SECURITY_CHECKLIST.md` 新增本机绝对路径扫描。
2. 泄漏已进入 git 历史：公开仓库历史整体重写为干净快照（旧提交不可达）；全新克隆验证工作区与历史均无 `/Users/xiaowuliao` / `自媒体发布agent` 命中；main 分支保护重写期间临时关闭后已恢复（6 项必检 + 禁强制推送）。
3. CI 未跑 pytest：`security.yml` 新增 `Tests` job（pip install -r requirements.lock + pytest），分支保护必检项同步加入。
4. 补充 `WHITEPAPER.md`（22 条规则判定逻辑、授权密码学设计、安全覆盖、FAQ）并升级公开仓库 README（徽章/架构/安全声明）。

整改后本地门禁：bandit=0、semgrep=0、gitleaks=0、check_public_repo=PASSED、公开快照 34 测试全过、私有仓库 151 测试全过。
