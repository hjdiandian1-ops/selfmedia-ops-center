# 用户视角验收报告（2026-08-14）

> 按「全新用户：安装 → 使用 → 发布」的视角走查公开仓库，记录问题并修复。结论：**公开仓库已可「clone → 一键安装 → 浏览器打开工作台」直接使用**。

## 一、本次交付清单

| 交付 | 说明 |
|---|---|
| ⚙ 设置界面（前端） | 左下角新增「设置」按钮：LLM API Key / 地址 / 模型 + 公众号 AppID/Secret，保存到本地 .env（600 权限、掩码显示、留空不改）、一键「测试 AI 连接」 |
| 发布链路 | 成品库新增「推送到公众号草稿箱」（Pro + 认证号 + 凭据配置）与「发布指引」弹窗 |
| 发布操作手册 | `docs/发布与后台配置.md`：小红书/公众号（手动+自动）/短视频、认证判断、IP 白名单、48h 数据回填 |
| 引擎配置 | 设置保存后即时生效（无需重启）；无 Codex 时自动走 API 模式 |

## 二、用户视角走查记录（全新 clone 实测）

### 安装
1. `git clone` 公开仓库 → 82 个文件，结构完整 ✅
2. `python3 scripts/workbench_install.py` 自动建环境/装依赖/生成 .env ✅
3. `./start.sh` 启动并自动打开浏览器 ✅（走查用等价命令启动 uvicorn 验证）

### 使用
- 首页与 9 个核心接口全部 200：工作台 / 设置 / 授权状态 / 选题 / 爆款 / 统计 / 任务 / 发布手册 / 去AI味规则 ✅
- 免费用户（无授权文件）：选题、爆款榜单、质检正常；一键生产返回「Pro 功能，需要订阅授权」提示；爆款拆解按 3 次/月额度放行 ✅
- 设置保存：写入 .env、掩码显示、保存后引擎状态立即变为 API 就绪 ✅

### 发布
- 小红书：复制标题/正文 + 手动发布 + 标记已手动发布 + 48h 回填，路径清晰 ✅
- 公众号：手动模式（复制排版 HTML 到公众平台）人人可用；自动推草稿需要「已认证公众号 + AppID/Secret + IP 白名单」，未配置时返回明确中文提示 ✅
- 短视频：分镜脚本 → 拍摄/剪辑 → 上传，手册已写明 ✅

## 三、走查发现并修复的问题

| 问题 | 修复 |
|---|---|
| 小白不知道 .env 怎么配 | 前端 ⚙ 设置弹窗，掩码+测试连接 |
| 公众号推草稿完全没有入口和说明 | 成品库按钮 + `/api/publish/gzh-draft` + 发布手册（含认证判断、IP 白名单步骤） |
| 根目录 .env 会被 nas-n8n/.env 旧配置覆盖 | nas_config 改为根 .env 最后覆盖 |
| 模型类定义顺序导致启动报错 | 调整声明位置 |
| 设置测试用例含假密钥触发扫描告警 | 用例改为拼接字符串，扫描全绿 |
| CI 默认 CodeQL 与自定义 workflow 冲突 | 必检项改为真实的 `CodeQL (python)` |

## 四、验证数据

- 私有仓库测试：154 个全部通过（新增设置/草稿 4 项）
- 公开快照测试：65 个全部通过
- 发布前安全门禁：路径扫描 PASSED、gitleaks 0、bandit 0、semgrep 0
- GitHub CI：PR #7 六项必检全部通过并已合并（main `bf6d5240`）

## 五、已知边界（需要真实环境验证）

1. **真实 LLM Key 未实测**：测试用的是假 key（错误路径验证过）；建议用真实 DeepSeek/OpenAI Key 在设置页点一次「测试 AI 连接」；
2. **公众号自动推草稿依赖认证号**：你的账号若未认证，只能手动发布（手册已写明）；
3. **热点采集需要公网访问**：RSSHub/谷歌趋势/X 等源在无网络时显示失败，工作台仍可用（展示失败状态）。
