# 自媒体运营工厂 · 架构评估与优化方案

> 评估时间：2026-08-04
> 评估依据：agent.md / README.md / TODO.md / workflows/ 全部文档 / 6 大 Skills 源码 / scripts/ 工具链 / 最近一次完整产出（2026-08-04_Agent副业创收实战指南）及其素材包
> 评估目标：从"多角色扮演"升级为"真正的自动化工厂"，根治内容质量短板

---

## 一、总体结论（先看这里）

**当前系统是一个"文档完备的演示架构"，距离"自动化工厂"有三个根本性缺口：**

| 缺口 | 症状 | 后果 |
|---|---|---|
| **缺口 1：质量契约缺失** | 岗位之间靠"文档描述"传递信息，没有结构化输入/输出契约 | **素材衰减**——好素材在流转中被抽象化、丢弃 |
| **缺口 2：质量门形同虚设** | harsh-critic 只罚不奖、无评分细则，85 分阈值无法实际计算 | 质量闭环从未真正运转过 |
| **缺口 3：自动化只到"半自动"** | 定时触发、热点雷达、数据回收均未落地；发布主链路不稳 | 每次运行都靠人肉驱动，无法"无人值守" |

**最直接的证据（素材衰减）**：
2026-08-04 的素材包里有大量高冲击力具体素材——「合同盲审代运行 29 元/单 vs 律所 500 元」「行业数据订阅月收 99 元」以及 3 条爆款 Hook（"写了10个Agent却没赚到1块钱？"）。但最终三平台成稿**一个具体数字都没用、一条 Hook 都没引用**，全部退化为抽象概括（"分享三个我们团队验证过的真实落脚点"）。素材包是 80 分，成稿是 55 分——**质量不是生成环节差，而是流转环节丢的**。

---

## 二、架构设计评估

### 2.1 当前架构的真实形态

文档描述的架构：8 大岗位 + Supervisor 路由 + 物理 Subagent 进程 + LangGraph 质量闭环。

**实际运行的架构**：单一 LLM 在一次对话中顺序扮演 8 个角色。这带来三个结构性问题：

1. **无状态**：每个"岗位"没有独立上下文，前一个角色的输出以自然语言混在对话流里，后一个角色"看着办"地消费——信息保真度逐环衰减。
2. **无契约**：采编交付物（素材包）和主编输入之间没有 schema。主编"可以参考"素材包，也可以不看——没有任何机制强制。
3. **无回路**："85 分打回重写"在文档里存在，但没有可执行的评分细则，实际上从未触发过。

### 2.2 目标架构：从"角色扮演"到"流水线 + 契约"

```
┌─────────────────────────────────────────────────────────────────┐
│  Job 状态机（每个选题一个 Job 文件夹 + state.json）                │
│  状态：选题 → 素材 → 初稿 → 视觉 → 审核 → 归档 → 发布 → 回收       │
└─────────────────────────────────────────────────────────────────┘
         │                │                │
         ▼                ▼                ▼
   【输入契约】       【处理规则】       【输出契约 + 质量门】
   每个岗位的输入必须是结构化文件（带 frontmatter），
   输出必须通过校验才能进入下一状态。
```

**关键设计决策**：
- **Job 文件夹即状态**：`jobs/YYYY-MM-DD_主题/` 内含 `state.json`（当前状态、各环节质量分、打回次数），任何环节失败可从断点恢复，而非重跑全流程。
- **契约即 frontmatter**：素材包每条素材带 `source_type`（真实数据/用户投喂/AI推断）和 `priority`（核心/辅助）；成稿 frontmatter 必须列出 `consumed_materials`（引用了哪些素材条目）。机器可校验，不再靠自觉。
- **质量门可计算**：见第四节 harsh-critic 重构方案——双轨评分，每项指标可数、可算。

### 2.3 架构评分

| 维度 | 得分 | 说明 |
|---|---|---|
| 文档完备度 | 9/10 | 岗位、流程、指令模板覆盖全面 |
| Skills 资产质量 | 8/10 | guizang/gzh-design/xiaowan 三个排版视觉 Skill 是工业级水准 |
| 岗位契约化 | 2/10 | 纯自然语言传递，无 schema |
| 质量门可执行性 | 2/10 | 只罚不奖、无细则、未运转 |
| 自动化程度 | 3/10 | 定时/雷达/回收均未落地，发布靠降级链路 |
| **综合** | **4.8/10** | **骨架好、神经断——缺的是契约和闭环，不是更多角色** |

---

## 三、每个 Agent 的问题诊断与优化方案

### 3.1 总编（Orchestrator）
- **现状问题**：只是 prompt 角色，无调度能力；"决策卡点"（30 分钟未回复自动选第 1 题）没有实现机制。
- **优化方案**：
  1. 落地 Job 状态机：用 `state.json` 跟踪每个选题的流转状态，支持断点续跑。
  2. 定时触发落地：用系统 automation（08:00/12:00/20:00）真正拉起工作流，而非文档里的口头约定。
  3. 决策卡点超时机制：定时任务里写死"无回复则选热度第 1"，可执行。

### 3.2 资深采编（Senior Researcher）
- **现状问题**：① 素材包质量尚可但**混入虚构案例**（"29 元合同盲审"是 AI 编的，而 style-guide 和 harsh-critic 都明令禁止——采编阶段就违规，下游全部中毒）；② 素材无来源标注，下游无法区分真伪；③ rsshub 热点雷达部署了但数据没有流入选题。
- **优化方案**：
  1. **素材三级来源标注（强制）**：每条素材必须标 `source_type: 真实数据 / 用户投喂 / AI推断`。`AI推断` 条目禁止作为事实引用，只能作为观点启发。
  2. **核心素材标记**：采编必须标记 3-5 条 `priority: 核心` 的素材，下游主编强制消费（质量门校验）。
  3. 接入真实数据源：rsshub 热点 + WebSearch 真实案例，减少 AI 脑补。
  4. 引入 dbskill 的 `dbs-deconstruct`（解构案例库）做对标拆解，而非仅 benchmark。

### 3.3 小红书主编（Xiaohongshu Editor）
- **现状问题**：① 标题无公式支撑（08-04 的三个标题："不搞噱头：…"/"从套壳Chatbot到…"——无数字、无冲突、无身份代入，点击率上限低）；② 正文零具体数字，素材包里的 29 元/500 元/99 元全部没用；③ Hook 温和，无冲击力。
- **优化方案**：
  1. **接入 dbs-xhs-title（已在库中但未引用！）**：75 个验证过的爆款公式，12 类心理触发器。标题必须可追溯到公式编号，每次输出覆盖 ≥3 种触发器类型。
  2. **素材消费契约**：正文必须引用素材包中全部 `priority: 核心` 条目，成稿 frontmatter 列出 consumed_materials。
  3. 数字密度硬指标：小红书正文 ≥2 个具体数字（单价/对比/百分比）。

### 3.4 公众号主编（WeChat Editor）
- **现状问题**：① 开头就是典型 AI 腔（"过去一年里，市场上出现了成千上万个…"）——style-guide 明确禁止的句式；② 通篇无第一人称真实经历、无真实项目名，"实战操盘手"人设落空；③ 结构完整但像通用科普文。
- **优化方案**：
  1. **真实感检查清单（写入岗位 SOP）**：每篇必须含 ≥2 个具体数字、≥1 个真实项目/工具名、≥1 段第一人称经历或观察（来自用户投喂种子素材；无种子素材时必须向用户索取，而非脑补）。
  2. 开头校验：对照 style-guide 的 Hook 句式 A/B/C，禁用"在过去…/随着…/市场上出现了…"开头。
  3. 范文 few-shot：从 19 篇历史文章里选 2-3 篇数据最好的做段落级解剖（为什么这段这样写），放入 style-guide——**示例比规则有效 10 倍**。

### 3.5 短视频导演（Video Director）
- **现状问题**：分镜台词 = 文章缩写版，没有独立的 Hook 设计；viral-content-skill 的分镜模板固定五段式，每篇 Hook 句式雷同。
- **优化方案**：
  1. **接入 dbs-hook（已在库中未引用！）**：开头 = 话题 + Hook + 可信度三要素公式；先诊断素材丰富度（5 维度检查），素材不够时停止优化并要求补料，而非硬写。
  2. 0-3s Hook 必须独立设计，禁止压缩文章第一句。
  3. 口播友好检查：台词逐句过"能直接念出来吗"测试。

### 3.6 美术总监（Visual Director）
- **现状问题**：guizang skill 本身工业级完善；问题在于**双轨路由规则缺失**——什么时候用 HTML 卡片、什么时候用 AI 生图，没有决策依据；渲染后 validate 脚本不是默认执行。
- **优化方案**：
  1. 明确路由：知识/数据密集页 → HTML 卡片轨；情绪封面/首图 → AI 生图轨。封面 + 内容页混合时以 HTML 卡片为主。
  2. `validate-social-deck.mjs` 和 4 横带密度检查改为默认执行（当前是"用户要求才跑"）。
  3. 渲染产物命名规范并入 Job 文件夹契约。

### 3.7 资深校对排版（Chief Reviewer）
- **现状问题**：这是**全系统最薄弱的一环**。harsh-critic 只有 8 条扣分项：只罚不奖（没有"有具体数据加分"这类正向指标）、85 分无可计算细则、且与实际产出矛盾（产出明显未到 85 分却通过了）。dbs-resonate / dbs-spread 声称要用但实际没跑。
- **优化方案（重构为双轨评分，详见 4.1）**。

### 3.8 归档发布员（Asset & Distribution Ops）
- **现状问题**：① `publish_to_n8n.py` **硬编码 NAS 密码**（安全隐患）；② n8n webhook 主链路常年 404，实际靠降级链路直连 xhs_publisher——scripts/ 目录 70 个 fix_* 脚本就是这个链路反复修复的考古现场；③ 发布后无数据回收。
- **优化方案**：
  1. 密码移入 `.env` / 环境变量，脚本从 `nas-n8n/.env` 读取。
  2. 修复或放弃 n8n webhook 主链路：既然降级链路是事实上的主链路，就把直连设为 primary、n8n 降为可选，减少无效重试（符合铁律：同一问题不试第 4 次）。
  3. 归档时写入 `publish_log.json`（发布时间、平台、初始数据），为数据回收打底。

---

## 四、Skills 优化方案

### 4.1 harsh-critic-skill（最高优先级重构）

**从"扣分清单"重构为"双轨可计算评分"：**

```
总分 = 正向质量分（60 分）+ 负向扣分（40 分）

【正向质量分 60 分】
- 素材引用率（20 分）：素材包 priority=核心 条目被引用比例 ×20
- 数据密度（15 分）：具体数字（单价/百分比/对比）每处 3 分，上限 15
- 真实感（15 分）：第一人称经历/真实项目名/真实对话，每处 5 分
- Hook 冲击力（10 分）：对照 dbs-hook 六维检查表，过 1 维 2 分

【负向扣分 40 分】（保留并收紧现有项）
- 营销号套话/爹味 -20/处
- 虚构案例（AI推断素材被当作事实引用）-30/处
- AI 腔开头（"在过去/随着/市场上出现了"）-15
- 孤行/无功能分割线 -10/处

判定：≥85 通过；<85 输出逐项失分明细退回对应主编。
```

**新增"成稿 vs 素材包"对照检查步骤**——这是直接针对素材衰减问题的机制：critic 必须并排读素材包和成稿，逐条核对核心素材去向。

### 4.2 viral-content-skill
- Hook 句式从"一套模板"改为"3-5 套变量库"，避免每篇雷同；
- 分镜模板加入"素材绑定"字段：每段台词标注引用了素材包哪条素材；
- 与 dbs-hook 打通：0-3s Hook 必须过话题+Hook+可信度公式。

### 4.3 personal-style-guide.md
- **加入范文解剖区**：2-3 篇历史高数据文章的段落级标注（这段为什么是 Hook、这段为什么放数据）；
- **修复内部矛盾**：style-guide 禁止虚构案例，但采编 SOP 没有来源标注机制导致违规素材流入——补"素材来源三级标注"规范（与 3.2 对齐）；
- 补充"禁用开头句式清单"（"在过去 N 年…/随着…的飞速发展/市场上出现了…"）。

### 4.4 guizang-social-card-skill
- 资产质量已是工业级，无需大改；两处集成优化：① 产物落盘路径并入 Job 契约；② validate 默认执行。

### 4.5 gzh-design-skill / xiaowan-wechat-layout-skill
- 两者定位清晰、资产完善，保持不变；唯一建议：把 xiaowan 的"反馈路由"结果（用户每次排版反馈）定期回写为个人偏好文件，跨篇复用。

### 4.6 dbskill（利用率最低的宝库）
- **现状**：30 个子技能只引用了 4 个（benchmark/content/resonate/spread）。
- **立即补充引用**：
  - `dbs-xhs-title` → 小红书主编（标题公式库，直接解决最大短板）
  - `dbs-hook` → 短视频导演 + 公众号主编（开头诊断）
  - `dbs-deconstruct` → 资深采编（对标案例解构）
- 更新 agent.md 的 Skills 清单，把这 3 个写入对应岗位的"必须加载"。

---

## 五、通往自动化工厂的三阶段路线图

### Phase 1：质量地基（1-2 天，立即做）
| # | 动作 | 解决什么 |
|---|---|---|
| 1 | 重构 harsh-critic 为双轨评分 + 素材引用率硬指标 | 质量门形同虚设 |
| 2 | 素材包 schema 化（source_type / priority 标注） | 虚构素材流入 + 素材衰减 |
| 3 | 成稿 frontmatter 契约（consumed_materials 清单） | 素材衰减可被机器校验 |
| 4 | agent.md 补充引用 dbs-hook / dbs-xhs-title / dbs-deconstruct | 标题和 Hook 最大短板 |
| 5 | publish_to_n8n.py 密码移入 .env | 安全隐患 |

**验收标准**：用同一主题重跑一次《Agent 副业创收》，成稿必须含素材包全部核心条目（29 元 vs 500 元、99 元订阅），harsh-critic 输出逐项评分明细。

### Phase 2：流程硬化（2-3 天）
| # | 动作 | 解决什么 |
|---|---|---|
| 6 | Job 状态机落地（jobs/ 目录 + state.json，断点续跑） | 无状态、无法恢复 |
| 7 | 每日 08:00/12:00/20:00 定时 automation 真正拉起 | 人肉驱动 |
| 8 | rsshub 热点 → 选题推荐数据流打通 | 热点雷达空转 |
| 9 | 发布链路简化：直连设为主链路，n8n 降级 | 无效重试循环 |

### Phase 3：数据闭环（持续运营）
| # | 动作 | 解决什么 |
|---|---|---|
| 10 | 发布后 48h 数据回收（阅读/点赞/收藏 → publish_log.json） | 无反馈，风格无法进化 |
| 11 | 爆款反哺：数据好的文章解剖后回写 style-guide 范文库 | 风格指南静态不进化 |
| 12 | 周度质量复盘：harsh-critic 历史评分趋势 + 高频失分项归因 | 质量不可度量 |

---

## 六、一句话总结

> **现在的系统不缺角色、不缺文档、不缺 Skill 资产——缺的是岗位之间的"契约"和质量门的"牙齿"。先把素材流转的契约钉死、把评分做得可计算，质量就能立马上一个台阶；自动化是第三步的事。**
