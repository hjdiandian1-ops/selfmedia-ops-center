# 自媒体运营平台 · 项目评审与优化报告

> 评审时间：2026-08-20 ｜ 范围：`webapp/` + `scripts/` 主目录（不含 `release/` 快照与第三方 `skills/`）
> 结论先行：**架构骨架已经是"能打的量产级"，但「无人值守」「真实素材」「反馈闭环」三块存在实打实的断点，本次已逐一修复。**

---

## 一、成熟度总评

| 维度 | 评级 | 说明 |
|---|---|---|
| 后端架构 | ⭐⭐⭐⭐ | server.py 已拆 9 个 Router + core 共享层，职责清晰 |
| 生产流水线 | ⭐⭐⭐⭐ | 4 阶段解耦、断点续跑、质检自愈，设计是亮点 |
| 质检体系 | ⭐⭐⭐⭐ | 契约 / Harsh / 去AI味 / 合规四道门禁可机器计算 |
| 前端体验 | ⭐⭐⭐⭐ | 8 主题、响应式、首启向导、SVG 折线图，完整度高 |
| 数据闭环 | ⭐⭐⭐ | 飞轮链路是通的，但反馈模型有 bug、无内置调度、无 token 计量 |
| 生产质量 | ⭐⭐⭐ | 素材阶段无真实研究、兜底会编造数字（本次已修） |
| 商业化 | ⭐⭐⭐ | 授权体系成型，但 README 4 个 TODO、截图等开源素材缺失 |
| 开源就绪度 | ⭐⭐ | 缺 Issue 模板 / CONTRIBUTING 增强 / CHANGELOG / 真实截图（即上次 Prompt 8） |

一句话：**它已经是一个"功能齐全的内容工厂原型"，距离"自用顺手 + 开源可信"还差：真定时、真素材、真反馈、真说明。**

---

## 二、你提出的 9 个问题 · 根因与结论

### 1. 选题模块：定时规则、自动采纳规则、右侧删除

- **定时规则**：真相是——**项目里根本没有内置定时器**。`run_daily_pipeline.py` 的 docstring 写的是"Agent 定时任务只需调用本脚本"，意思是它期待由外部 Agent（WorkBuddy/Codex 定时任务）来调它；webapp 内没有任何 cron / APScheduler / 后台线程。README 里"每日自动采集""周一自动聚合"与事实不符。
- **自动采纳**：默认就是"用户拍板"——只有点「采纳生产」才建 Job。`--auto-select`（自动选热度第 1 建 Job）只存在于命令行参数，**从未暴露到 UI**，所以不存在"偷偷自动生产"。
- **右侧删除**：生产队列没有任何删除接口，只能取消/重跑。
- ✅ **已修**：新增内置调度器（应用打开期间按点触发）+ 设置页「定时任务」面板；新增 `DELETE /api/production/{job_id}`，选题页「进行中/最近采纳」与流水线队列都加了「删除」按钮。

### 2. 爆款跟踪：定时规则、推断报告、经验包去向

- **定时规则**：同问题 1，无内置定时器。✅ 已修（调度器里拆成独立的「三平台爆款榜单 + Top5 拆解」任务）。
- **为什么都是"推断报告"**：`collect_platform_virals.py` 只抓到了 **标题 + 热度 + 链接**，没有抓正文/逐字稿；`run_viral_analysis.py` 在无正文时只能基于标题推断，且旧代码没有把"依据级别"标出来，前端也原样展示，看起来就像"真分析"。
- **经验包去向**：真实去向是 `data/flywheel/viral_weekly_<周>.md` + `lessons.json`（写经验库）+ 自动升级 `agents/*.md` 三份 SOP。但前端 toast 只说"已生成"，没告诉你去哪了。
- ✅ **已修**：拆解报告显著标注「📄 真实分析（有原文）/ 🔎 推断拆解（仅标题）」，并提示怎么补原文重拆；「生成本周经验包」toast 现在会显示写入路径与升级的 SOP 文件名。

### 3. 数据飞轮：反哺指令包去哪了、有没有生效、哪里看效果

- **去向**：点「重新生成反哺指令包」会写入 `data/flywheel/pipeline_feedback.md`，同时调用 `upgrade_agent_docs.py` 把经验补丁写进 `agents/` 下各 SOP 文档的「🧬 经验补丁」章节（版本号 +1、追加 changelog）。
- **有没有生效**：**真的生效**。`run_production.py` 的 Stage 1（采编）和总 prompt 都会读 `pipeline_feedback.md` 并注入给模型，下次生产立即生效。这条链路是通的，只是"看不见"。
- **哪里看效果**：飞轮页右栏「反哺指令包」预览 + 路径已显示；流水线页「查看 SOP 文档」能看到经验补丁章节和版本号。
- ✅ **已修**：生成/聚合后的 toast 明确写出"写入哪个文件 + 升级了哪几份 SOP"。

### 4. 流水线：队列排序与归档

- **根因**：`data/production/queue.json` 按插入顺序返回；`/api/jobs` 按目录名（日期）**升序**，旧的排前面；没有"归档/折叠"概念。
- ✅ **已修**：后端统一按"运行中 → 排队中 → 其余按创建时间倒序"，超过 7 天的已完成/失败任务标记 `archived`；前端最新在前，超 1 周的自动折叠进「📦 已归档（N 条，点击展开）」。

### 5. 质检趋势与门禁、🎯 选题反馈模型的作用

- **质检趋势与门禁**：每篇产出归档前过 4 道机器门禁——契约校验（素材引用是否完整）、Harsh Critic 打分（≥80 分放行）、去 AI 味（结构级 AI 腔一票否决）、内容合规（广告法/平台红线）。`/api/qa/history` 把这些结果汇成走势图 + 高频失分 TOP5 + 里程碑，用来观察"写作质量是否在变好"。**这一块后端+前端都已完整实现**，只是页面没写说明。✅ 已补说明文案。
- **🎯 选题反馈模型**：它的作用是——用你回填的阅读/赞/藏数据，反推"哪个维度（质量/热度/时效/IP 垂直度）最影响你的账号表现"，然后自动微调日/周选题评分权重（幅度 ±30% 内防过拟合）。
- **严重 bug（已修）**：`topic_feedback.py` 里 `extract_topic_features()` 调用 `score_item("回填样本", 1, theme, "", 0.0)`，但 `score_item(title)` 只接受 1 个参数 → **每次都抛 TypeError 被 except 吞掉** → 所有主题特征都是同一套默认值 → 皮尔逊相关恒为 0 → 你看到的校准报告是"假的、无效的"。✅ 已重写该函数，质量/IP 两个维度现在会随主题真实变化。
- 注意：需要 **≥10 条有效回填样本** 才会真正调权，否则维持默认权重（这是设计，不是 bug）。

### 6. 设置菜单：AI 引擎参数不能改/删、模式选择、token 消耗

- **能不能改**：后端 `POST /api/settings` 本来就是覆盖写入（能改），但**前端不加载现有引擎状态、不显示当前模型、没有"清空"入口**，所以你"看不到改没改成"。
- **模式**：代码里其实只有"自动探测 codex 否则 api"，没有显式选择；且 README 说"装了 Codex 优先用 Codex"，但**生产流水线实际只用 `llm_engine.chat`（API 直连）**，只有爆款拆解走 Codex。文档与实现不一致。
- **token 消耗**：从未统计。
- ✅ **已修**：设置页新增「引擎接入模式」下拉（自动 / API 直连 / Codex CLI / WorkBuddy），显示当前引擎与当前模型；`llm_engine` 每次调用累计 token 消耗落盘 `data/llm_usage.json` 并在设置页展示；新增「清空 LLM 配置」按钮（`POST /api/settings/llm-clear`）。

### 7. 8 月 19 日「AI 客服国标」主题质量下降的原因

这是本次最值得说清楚的一个问题，**不是玄学，是 4 个叠加的根因**：

1. **素材阶段根本没有"研究"**。`stage_1_materials` 只是让 LLM 凭空"生成一份结构化素材包"，既没有联网抓取，也没有把你已经抓到的热点雷达/榜单喂给它 → LLM 只能靠记忆编，具体数字全凭幻觉。
2. **兜底模板在编造数据**。旧代码里 LLM 一旦失败/超时/没配 key，会写入一个写死的"降级模板"，里面赫然是"效率提升超过 80%""错误率下降 65%""降本 70% 以上"这种**假数据**。你看的 8-19 那篇如果命中这条路径，产出必然又空又假。
3. **个人文风指南被截断错位**。旧代码传的是 `style_guide[-1500:]`（**最后 1500 字**），而文风指南的核心规则（人设/口吻/红线）通常在**开头**，等于把最重要的部分切掉了，只喂了尾巴。
4. **生产走的是 API 直连而非 Codex**。你之前效果好的文章大概率是 Codex 带联网/工具链跑出来的；现在流水线落到 `deepseek-chat` 纯文本补全，能力上限不一样。

✅ **已修**：① 素材阶段注入"最近一次真实热点雷达 + 选题推荐 + 已沉淀经验"做 grounding；② 兜底模板不再编造任何数字，缺失处明确写"数据待核实，禁止编造"；③ 文风指南改传开头核心部分；④ （见下方建议）后续可把 Codex 真正接回生产主链路。

### 8. 我额外发现的问题

- **README 与实现不符**：多处"每日自动采集 / 周一自动聚合 / 优先 Codex"是愿景不是现状，建议改成"内置定时器（设置里开启）"。
- **`run_daily_pipeline.py --topics` 把"选题"和"爆款采集+拆解"耦合**在一起，没法分别定时；本次调度器已拆成两个独立动作。
- **前端「热点采集」按钮走同步 `run_script`（timeout 300s）**，会长时间占住 HTTP 连接；调度器已改为独立后台执行。
- **`release/selfmedia-ops-center/` 是发布快照**，本次只改了主目录，正式发版前需要同步。
- **GitHub 开源素材（上次 Prompt 8）仍未做**：截图、Issue 模板、CHANGELOG、README 4 个 TODO 都还空着。

---

## 三、本次已落地的代码改动

**新增文件**
- `webapp/scheduler.py` —— 内置定时调度器（配置 `data/scheduler.json`，默认关闭）
- `webapp/routers/scheduler.py` —— `/api/scheduler` 三个接口（读/存/立即运行）
- `data/scheduler.json` —— 默认调度配置（选题 8/12/20 点、爆款 9/21 点、周聚合周一 21 点、回收 21:30）

**修改文件**
- `webapp/server.py` —— 挂载调度 Router + 启动时拉起调度线程
- `webapp/core.py` —— 引擎状态返回偏好模式与当前模型
- `webapp/routers/settings.py` —— 引擎模式保存、token 用量返回、`/api/settings/llm-clear`
- `webapp/routers/production.py` —— 队列倒序+归档标记、`/api/jobs` 倒序、`DELETE /api/production/{job_id}`
- `scripts/llm_engine.py` —— token 用量统计 + 引擎模式读取
- `scripts/run_production.py` —— 真实热点 grounding、去虚构兜底、文风指南截断修复
- `scripts/topic_feedback.py` —— 修复特征提取 bug（校准从此真实有效）
- `scripts/run_viral_analysis.py` —— 拆解报告标注依据级别（真实/推断）
- `webapp/static/index.html` —— 设置页「定时任务」面板、AI 引擎面板、质检/反馈说明
- `webapp/static/app.js` —— 队列折叠、删除按钮、引擎模式/用量渲染、经验包去向提示、调度器交互

**验证情况**：全部改动已通过语法检查（Python AST + `node --check`）；纯标准库逻辑（topic_feedback 特征、llm_usage、scheduler 配置读写）已跑通冒烟测试。完整的 `pytest tests/` 需要你本机 venv（沙箱无 fastapi/网络），建议你在本机跑一遍 `pytest tests/ -q` 确认。

---

## 四、接下来建议的优先级（未在本轮实现）

1. **把 Codex 真正接回生产主链路**：现在生产只用 API，质量上限被限制住。可在 `run_production.py` 加"codex 模式分支"，与爆款拆解保持一致。
2. **素材阶段接入真实搜索/正文抓取**：让采编真的去抓原文链接再写，而不是只喂榜单标题（爆款拆解的"推断"问题同源，抓正文才能根治）。
3. **GitHub 冷启动包（上次 Prompt 8）**：截图、CONTRIBUTING、Issue 模板、CHANGELOG、README 4 个 TODO。
4. **README 与现状对齐**：把"每日自动采集"等措辞改成"内置定时器（设置里开启）"，避免开源后被说"货不对板"。
5. **`release/` 快照同步** + 加一条 `pytest tests/ -q` 进你的提交前检查。
